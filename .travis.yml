dist: xenial

install:
  - pip install pipenv
  - pipenv install

language: python

python:
  - '3.7'

script:
  - cp pyre-check.sh.dist pyre-check.sh && ./pyre-check.sh
  - python -m unittest discover -s tests -p '*_test.py'

sudo: true
